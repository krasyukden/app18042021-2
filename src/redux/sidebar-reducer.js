
let initialState = {
    sidebarItem: [
        { id: 1, friend: "Andrew" },
        { id: 2, friend: "Max" },
        { id: 3, friend: "Dima" }
      ]
};

const sidebarReducer = (state = initialState, action) => {
    return state;
}
export default sidebarReducer;